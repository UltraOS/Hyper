root = true

[*]
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
charset = utf-8

# Markdown uses two trailing spaces as a hard line break, so keep them.
[*.md]
trim_trailing_whitespace = false
